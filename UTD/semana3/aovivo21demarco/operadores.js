//aritméticos (precedência) % * / + -

//var media = (10 + 8 +6) / 3;

//console.log(media % 3);

//operadores relacionais == != === !== > >= < <=

//operadores lógicos && || 

//operadores unários = ++ !

//operador ternário (condição) ? (valor se verdadeiro) : (valor se falso)

//var idade = 18;

//var podeDirigir = (idade >=18) ? "pode dirigir" : "não tem idade para dirigir";

//console.log(podeDirigir);   

for(var x=0; x<80; x+=10);
console.log(x);